import { TelaEmBreve } from '@/componentes/TelaEmBreve';

export default function Perfil() {
  return (
    <TelaEmBreve
      titulo="Meu Perfil"
      rotaAtiva="perfil"
      subtitulo="Gerenciamento de dados cadastrais, preferências de tema e configurações de conta."
    />
  );
}
